apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: essafwa-admin-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - admin.essafwa.com
      secretName: essafwa-admin-tls-secret
  rules:
    - host: admin.essafwa.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: essafwa-admin
                port:
                  number: 80
