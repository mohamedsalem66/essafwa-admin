apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: elemana-admin-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - admin.elemanaoptique.com
      secretName: elemana-admin-tls-secret
  rules:
    - host: admin.elemanaoptique.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: elemana-admin
                port:
                  number: 80
